# v5.0.0 Regression Checklist — 回归验证清单

**Date**: 2026-04-13  
**Version**: 5.0.0

---

## 1. Golden Path Verification

**目标**: 验证完整主线可运行。

**步骤**:
```bash
python scripts/regression_v500.py
```

**预期结果**:
- ✅ Experiment 创建成功
- ✅ Job 完成成功
- ✅ Artifact 创建成功
- ✅ Evaluation Ready Gate → passed
- ✅ Artifact Ready Gate → passed
- ✅ Promotion Ready Gate → passed
- ✅ Promote → approval_required
- ✅ Approve → approved
- ✅ Release Ready Gate → passed
- ✅ Seal → sealed
- ✅ Seal Ready Gate → passed

**失败处理**:
- 检查 workflow_jobs 状态和 error_message
- 检查 gate_checks 表中 blocked 记录
- 检查 audit_logs 中失败事件

---

## 2. Gate Interception Verification

**目标**: 验证 Gate 能正确拦截不满足条件的操作。

**步骤**:
1. 尝试 promote 一个已 promoted 的 artifact
2. 尝试 seal 一个未 approved 的 artifact
3. 尝试 seal 一个已 sealed 的 artifact

**预期结果**:
- ✅ Promotion Ready Gate 返回 blocked
- ✅ Release Ready Gate 返回 blocked
- ✅ API 返回 400 错误

---

## 3. Factory Status Verification

**目标**: 验证工厂运行态聚合正确。

**步骤**:
```bash
curl http://localhost:8787/api/factory/status | python -m json.tool
```

**预期结果**:
- ✅ mainline_health 各字段非空
- ✅ blocked_gates 列表存在
- ✅ recent_releases 列表存在
- ✅ recent_backups 列表存在

---

## 4. Backup Consistency Verification

**目标**: 验证备份一致性。

**步骤**:
```bash
python scripts/recovery_verify.py
```

**预期结果**:
- ✅ Release Package manifest 完整
- ✅ Lineage chain 完整
- ✅ Metrics snapshot 存在
- ✅ Approval record 存在
- ✅ Audit trail 存在

---

## 5. Recovery Drill Verification

**目标**: 验证恢复流程可行。

**步骤**:
```bash
python scripts/recovery_verify.py --drill
```

**预期结果**:
- ✅ 所有 verification 通过
- ✅ recovery_logs 记录创建

---

## 6. Build Verification

**目标**: 验证前端构建无错误。

**步骤**:
```bash
cd apps/web-ui && npm run build
```

**预期结果**:
- ✅ Build 成功
- ✅ 无 TypeScript 错误

---

## 7. API Health Verification

**目标**: 验证 API 正常运行。

**步骤**:
```bash
curl http://localhost:8787/api/health
```

**预期结果**:
- ✅ service: local-api
- ✅ database: ok

---

## Checklist Summary

| # | Verification | Pass Criteria | Result |
|---|--------------|---------------|--------|
| 1 | Golden Path | 22/22 passed | ⬜ |
| 2 | Gate Interception | blocked returned | ⬜ |
| 3 | Factory Status | ok=true | ⬜ |
| 4 | Backup Consistency | all checks passed | ⬜ |
| 5 | Recovery Drill | logged success | ⬜ |
| 6 | Build | success | ⬜ |
| 7 | API Health | ok=true | ⬜ |

**Sign-off**: _______________ Date: _______________
