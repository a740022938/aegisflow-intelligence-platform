# 视觉支线挂档与主线恢复说明

> 文档编号：M1-RESUME-001  
> 生成时间：2026-04-15 05:49 GMT+8  
> 状态：**已挂档 / 主线已恢复**

---

## 一、视觉支线当前进度

### 已完成阶段

| 阶段 | 状态 | 完成时间 | 核心产出 |
|------|------|----------|----------|
| T1 注册与接口预留 | ✅ PASSED | 2026-04-14 | vision-bus 管线目录注册（mahjong_detect / mahjong_classify / mahjong_fusion）、artifact 类型注册、I/O contract 文档 |
| T2 数据集接入 | ✅ PASSED | 2026-04-14 | 干净数据集 3214 对（clean/）、classify 格式 20922 张（classify/）、datasets 表已登记 |
| T3 样板链路验证 | ✅ PASSED | 2026-04-15 | detect dry-run 跑通、classify 2 epoch 训练跑通、50ep 正式训练 epoch 1-2 完成、GPU 已验证可用 |

### 当前暂停点

- **T3 已闭环**，50 epoch 训练在 epoch 2 结束时按口令终止
- **训练指标**：epoch 2 → top1=14.8%, top5=43.3%, train_loss=3.240（方向正确，未达可用精度）
- **暂停原因**：口令收口，不进入正式精度冲刺

### 未完成阶段

| 阶段 | 状态 | 说明 |
|------|------|------|
| T4 评估归档与发布 | ❌ 未启动 | 需要 ≥50 epoch 完整训练 + mAP/precision/recall/top1 指标归档 + release note |
| T5 UI/API 对接 | ❌ 未启动 | 需 T4 完成后，将麻将识别结果接入 Tasks/Training/Models/Dashboard 前端页面 |

---

## 二、视觉支线保留产物

### 数据产物

| 路径 | 大小 | 说明 |
|------|------|------|
| `E:\AGI_Factory\data\mahjong\clean\` | 381 MB | detect 格式数据集（3214 对 images/labels，train/val/test split） |
| `E:\AGI_Factory\data\mahjong\classify\` | 1.88 GB | classify 格式数据集（20922 张，34 类子目录，含 cache） |

### 训练产物

| 路径 | 大小 | 说明 |
|------|------|------|
| `runs/classify/mahjong_cls_v2/weights/best.pt` | 2.9 MB | 2ep 热身最佳权重（batch=16） |
| `runs/classify/mahjong_cls_v2/weights/last.pt` | 2.9 MB | 2ep 热身最末权重 |
| `runs/classify/mahjong_cls_50ep/weights/best.pt` | 8.6 MB | 50ep 正式训练最佳权重（epoch 2） |
| `runs/classify/mahjong_cls_50ep/weights/last.pt` | 8.6 MB | 50ep 正式训练最末权重（epoch 2） |
| `runs/classify/mahjong_cls_50ep/results.csv` | — | 训练指标记录 |
| `runs/classify/mahjong_cls_50ep/args.yaml` | — | 完整训练配置快照 |
| `runs/classify/mahjong_cls_v2/` | — | 可视化图（train/val batch） |
| `runs/mahjong_t3_dryrun/` | — | detect dry-run 配置+输出 |

### 脚本产物

| 路径 | 说明 |
|------|------|
| `workers/python-worker/mahjong_classify_train.py` | classify 训练脚本（detect→classify 格式转换 + YOLOv8n-cls 训练） |
| `workers/python-worker/preflight.py` | v3.x 通用+麻将专项预检脚本 |

### 文档产物

| 路径 | 说明 |
|------|------|
| `docs/vision/麻将模块_T1_注册与接口预留说明.md` | T1 结案文档 |
| `docs/vision/麻将模块_T2_数据集接入说明.md` | T2 结案文档 |
| `docs/vision/麻将模块_T3_结案报告.md` | T3 结案报告 |
| `docs/vision/麻将模块_T3_样板链路接入说明.md` | 链路接入说明 |
| `docs/vision/麻将模块_vision_io_contract_v1.md` | I/O 契约文档 |
| `docs/vision/麻将数据标准_v1.md` | 数据标准文档 |
| `docs/vision/麻将_dataset_versioning_v1.md` | 数据集版本管理文档 |

### 审计产物

| 路径 | 说明 |
|------|------|
| `audit/mahjong_t1_registration_check.md` | T1 注册审计 |
| `audit/mahjong_t2_dataset_check.md` | T2 数据集审计 |

### 数据库登记

- `datasets` 表：dataset_id = `c6b54bae-5384-4c5e-a935-473727c7a31a`，dataset_code = `mahjong_v1`，version = `mahjong_v1_20260415_001`，nc = 34

---

## 三、视觉支线重启条件

### 恢复训练（T3→T4）

```python
# 从 last.pt 恢复 50 epoch 训练
from ultralytics import YOLO
model = YOLO(r'E:\AGI_Factory\repo\runs\classify\mahjong_cls_50ep\weights\last.pt')
model.train(resume=True)
```

或从 best.pt 重新指定 epochs 启动新轮次。

### 精度目标

- 34 类分类 **top1_acc ≥ 85%** → 可用线
- 34 类分类 **top1_acc ≥ 95%** → 上线线

### 可选升级

- 换 `yolov8s-cls.pt`（5.4M 参数）提升精度和 GPU 利用率
- 当前 yolov8n-cls（1.48M 参数）对 RTX 3060 利用率仅 2%，CPU 是瓶颈

### T5 前置条件

- T4 完成（模型评估归档 + release note）
- 前端页面可读取 classifier 推理结果 API

---

## 四、主线恢复确认

- 视觉支线已挂档，所有产物保留，无删除操作
- 主线基线未被破坏（102 个未提交文件为视觉支线施工期间的工作文件）
- 主线状态：**v6.5.0，P4 stage1 pre-seal baseline**
- 下一步：执行 M1 主线状态盘点
