# 主线当前状态盘点

> 文档编号：M1-INVENTORY-001  
> 生成时间：2026-04-15 05:49 GMT+8  
> 版本基线：v6.5.0 | P4 stage1 pre-seal baseline

---

## 一、系统概览

| 维度 | 状态 |
|------|------|
| 当前版本 | v6.5.0 |
| Git 最新提交 | `2c84339` chore: freeze legacy yolo execution paths by default |
| 未提交变更 | 102 个文件（视觉支线施工期积累） |
| 运行时 | Node ≥22 + Python 3.11 + SQLite WAL |
| 数据库 | 3 个 .db 文件，均为 0 字节空库（schema 未 push） |
| 包管理 | pnpm 9.15.0 monorepo |

---

## 二、各模块盘点

### 2.1 Tasks（任务管理）

**状态：✅ 已稳定**

- Schema：tasks / task_steps / task_logs 三表完整
- API：CRUD + 任务执行（task-execute）+ 状态机
- 前端：Tasks.tsx 页面完整
- 工作流集成：支持 workflow job 编排（pending → running → completed/failed）

### 2.2 Templates（模板管理）

**状态：✅ 已稳定**

- API：模板 CRUD 路由已注册
- 前端：Templates.tsx 页面完整
- 工作流：workflow template 创建 → job 提交链路已通
- templates/ 目录为空（运行时创建，非文件系统模板）

### 2.3 Datasets（数据集管理）

**状态：⚠️ 基础稳定，麻将扩展已完成**

- Schema：datasets / dataset_versions 两表
- API：CRUD + dataset-pipeline（数据管线）
- 前端：Datasets.tsx 页面完整
- 麻将扩展：dataset_id = c6b54bae... 已登记，mahjong_v1 版本已注册
- **缺陷**：datasets schema 缺少 nc/class_names 等视觉专用字段，当前以 metadata JSON 存储

### 2.4 Training（训练管理）

**状态：⚠️ 基础稳定，视觉训练可用但未完全集成**

- API：training 路由已注册
- 前端：Training.tsx 页面完整
- Python worker：trainer_runner.py（YOLO detect）+ mahjong_classify_train.py（麻将 classify）
- **问题**：
  - trainer_runner.py 仅支持 yolo detect train，不支持 classify 模式
  - mahjong_classify_train.py 为临时脚本，未集成进 workflow executor
  - 训练产物（runs/）未自动注册到 models 表
  - 50 epoch 训练中途终止，无完整评估数据

### 2.5 Models（模型管理）

**状态：⚠️ 框架已有，数据空**

- Schema：models 表（id / name / type / framework / path / sizeBytes / metadata）
- API：models 路由已注册 + registerModelsRoutes 扩展
- 前端：Models.tsx 页面完整
- models/ 目录为空
- **实际模型文件散落**：
  - workers/python-worker/yolov8n.pt（检测预训练）
  - workers/python-worker/yolov8n-cls.pt（分类预训练）
  - runs/classify/mahjong_cls_50ep/weights/best.pt（训练产物）
  - 上述均未注册到 models 表

### 2.6 Audit（审计）

**状态：✅ 已稳定**

- Schema：audit_logs 表 + 审计路由
- 前端：Audit.tsx 页面完整
- 已有审计产物：P4 seal audit / YOLO phase-a final seal / 麻将 T1/T2 审计等
- 版本链完整：v4.2.0 → v6.4.0 各版本 closure report 均存档

### 2.7 Dashboard（驾驶舱）

**状态：✅ 已稳定**

- 前端：Dashboard.tsx 完整（工厂总览 + 活动流 + 插件状态）
- API：dashboard 路由提供汇总统计
- 三语支持：zh / en / ja

### 2.8 Vision Bus（视觉总线）

**状态：⚠️ 接口预留，部分激活**

- 已注册管线：9 条（3 frozen + 3 active + 3 planned）
  - frozen：yolo_detect / tracker_run / rule_engine
  - active：sam_handoff / sam_segment / classifier_verification
  - planned：mahjong_detect / mahjong_classify / mahjong_fusion
- 已注册 artifact 类型：11 种（含 3 种麻将专用）
- 中间结果查询 API：sam_handoffs / sam_segmentations / classifier_verifications / tracker_runs / rule_engine_runs

### 2.9 Workflow（工作流引擎）

**状态：✅ 已稳定**

- 状态机：Job → Step → 状态流转完整
- Step 状态机：9 种状态 + 合法转换规则
- 审批集成：step 级别审批（manual / auto_approve / auto_reject）
- 路由注册：registerWorkflowRoutes 已挂载

### 2.10 其他模块状态速览

| 模块 | 状态 | 说明 |
|------|------|------|
| Approvals | ✅ 稳定 | 审批引擎，create/approve/reject |
| Artifacts | ✅ 稳定 | 产物管理，类型注册 + CRUD |
| Evaluations | ✅ 稳定 | 评估管理 |
| Experiments | ✅ 稳定 | 实验管理 + patch_sets / sam_handoffs / sam_segmentations |
| Deployments | ✅ 稳定 | 部署管理 + targets / revisions |
| Rollback Points | ✅ 稳定 | 回滚点管理 |
| Knowledge | ✅ 稳定 | v6.1.0 知识中心 |
| Feedback | ✅ 稳定 | v6.3.0 反馈循环 |
| Cost Routing | ✅ 稳定 | v6.4.0 成本路由 |
| Outputs | ✅ 稳定 | 输出模板 |
| Ops | ✅ 稳定 | 运维操作接口 |
| Gates | ✅ 稳定 | 准入关卡 |

---

## 三、模块分类总结

### 已稳定模块（无需施工）

Tasks, Templates, Audit, Dashboard, Workflow, Approvals, Artifacts, Evaluations, Experiments, Deployments, Rollback Points, Knowledge, Feedback, Cost Routing, Outputs, Ops, Gates

### 仅预留接口（需要实装）

| 模块 | 预留程度 | 差距 |
|------|----------|------|
| tracker_run | API 查询接口已有 | 无实际跟踪逻辑、无 Python runner |
| rule_engine | API 查询接口已有 | 无规则推理逻辑、无 Python runner |
| mahjong_detect | vision-bus 已注册 | YOLO frozen，execution_mode 未实现 |
| mahjong_classify | vision-bus 已注册 | 训练脚本临时、未集成 workflow executor |
| mahjong_fusion | vision-bus 已注册 | 完全未实现，依赖 detect + classify |

### 仍缺施工（需要新建）

| 缺口 | 说明 |
|------|------|
| DB schema 未 push | 3 个 .db 文件均为 0 字节，schema 仅存在于 schema.ts 定义 |
| 训练产物 → models 自动注册 | 训练完成后需手动注册模型 |
| 训练全流程集成 | classify 训练未接入 workflow executor step |
| 评估归档自动化 | T4 需要自动从 runs/ 提取指标写入 experiments/experiment_metrics |
| 102 个未提交文件 | 视觉支线施工期间积累，需归档或清理 |

### 适合下一轮施工重点

1. **DB 初始化 + 数据填充**：schema push + 种子数据 + 麻将数据集正式写入
2. **训练全流程集成**：classify 训练接入 workflow executor → 训练完成自动注册 model → 自动归档评估
3. **技术债务清理**：102 未提交文件归档、551 个 .pre-restore/.bak 文件、runs/ 目录清理

---

## 四、技术债务清单

| 债务项 | 数量/大小 | 影响 | 优先级 |
|--------|-----------|------|--------|
| 未提交 Git 变更 | 102 文件 | 回归风险 | 高 |
| .pre-restore/.bak/.backup 文件 | 551 个 | 仓库膨胀 | 中 |
| runs/ 历史输出 | 3 个目录 | 磁盘占用 | 低 |
| DB 文件为空 | 3 个 .db = 0 bytes | API 无数据 | 高 |
| local-api build TS5011 | rootDir 配置问题 | 构建阻断 | 高 |
| 工作流路由重复注册 | 代码层面 | 运行时风险 | 中 |

---

## 五、OpenClaw 预留接入状态

- 当前无 OpenClaw 专用集成代码
- AGI Factory 的 API 端口 8787 可通过 HTTP 对外暴露
- WebSocket 端口 8788 已预留
- 展示站点 xlxai740022938.cc.cd 已部署（三语暗色科技风）
