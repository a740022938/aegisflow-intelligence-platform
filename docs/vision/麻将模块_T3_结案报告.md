# 麻将模块 T3 样板链路接入 — 结案报告

> 封板时间：2026-04-14 21:07 CST  
> 阶段状态：T1✅ → T2✅ → **T3✅（样板链路验证通过，训练阻塞于 GPU）**

---

## 1. T3 交付物

| # | 交付物 | 路径 | 状态 |
|---|--------|------|------|
| 1 | T3 接入说明文档 | `docs/vision/麻将模块_T3_样板链路接入说明.md` | ✅ |
| 2 | dry-run 配置快照 | `runs/mahjong_t3_dryrun/config.json` | ✅ |
| 3 | 环境快照 | `runs/mahjong_t3_dryrun/env_snapshot.json` | ✅ |
| 4 | 配置快照 | `runs/mahjong_t3_dryrun/config_snapshot.json` | ✅ |
| 5 | Preflight 预检脚本 | `python-worker/preflight.py` | ✅ |

---

## 2. Preflight 结果（10/10 全绿）

| 检查项 | 结果 |
|--------|------|
| dataset_version 格式 | ✅ `mahjong_v1_20260415_001` |
| 34 类校验 | ✅ 全部中文名齐全 |
| manifest 完整性 | ✅ train/val/test 均可解析 |
| 图像-标签配对 | ✅ 3214 对全部匹配 |
| split 比例 | ✅ train:2571 val:482 test:161 |
| 0KB label 风险 | ⚠️ 存在（augment 副作用，不阻断） |
| dataset.yaml | ✅ 可用 |
| class_map.json | ⚠️ 缺失（非阻断，manifest 内含 class_names） |
| 配置快照 | ✅ 已生成 |
| 环境快照 | ✅ 已生成 |

---

## 3. 干净数据集状态

**位置**：`E:\AGI_Factory\data\mahjong\clean\`

| split | 图片 | 标签 | 大小 |
|-------|------|------|------|
| train | 2571 | 2571 | ~377.6 MB |
| val | 482 | 482 | ~14.4 MB |
| test | 161 | 161 | — |
| **合计** | **3214** | **3214** | ~0.37 GB |

datasets 表登记：`dataset_id = c6b54bae-5384-4c5e-a935-473727c7a31a`

---

## 4. 阻塞项：GPU 不可用

**根因**：当前 PyTorch 为 CPU-only 版本（2.11.0+cpu），RTX 3060 无法被识别。

**影响**：T4 训练无法启动。CPU 模式下训练进程因下载卡住已终止。

**处置**：
- CUDA 版 PyTorch 安装命令已在 session `gentle-falcon` 中执行，结果未确认
- **必须**重装 CUDA 版 PyTorch 后验证 `torch.cuda.is_available() == True`
- 验证命令：`python -c "import torch; print(torch.cuda.is_available(), torch.cuda.get_device_name(0))"`

---

## 5. 非阻断风险

| 风险 | 影响 | 建议 |
|------|------|------|
| 0KB label 文件 | train 集部分样本无有效标注，影响有效样本数 | 训练前过滤或 YOLO 自动跳过 |
| class_map.json 缺失 | 非阻断，manifest 已含 class_names | 可选补充生成 |
| raw/ 目录未删除 | 占用 ~10 GB 磁盘空间 | 确认后删除 `E:\AGI_Factory\data\mahjong\raw\` |
| E:\AAA 空壳 | 原始数据目录 train 已移走，仅剩空结构 | 可安全删除 |

---

## 6. T4 启动条件

1. ✅ CUDA PyTorch 安装并验证 GPU 可用
2. ✅ `runs/mahjong_t3_dryrun/config.json` 可直接复用启动训练
3. ✅ 数据集 clean/ 完整，3214 对配对无误
4. ⬜ 确认 raw/ 删除释放空间（可选）

**T4 启动口令**（施工任务包定义）：
```bash
python models/train.py --execution_mode mahjong_classify --dataset_version mahjong_v1_20260415_001
```

---

## 7. 文件变更清单

| 操作 | 文件 |
|------|------|
| 新增 | `docs/vision/麻将模块_T3_样板链路接入说明.md` |
| 新增 | `runs/mahjong_t3_dryrun/config.json` |
| 新增 | `runs/mahjong_t3_dryrun/config_snapshot.json` |
| 新增 | `runs/mahjong_t3_dryrun/env_snapshot.json` |
| 新增 | `python-worker/preflight.py`（v3.x 含麻将专项预检） |

无删改，主线封板基线未受影响。
