# v5.2.0 Not In Scope — 明确不纳入范围

**Date**: 2026-04-13  
**Version**: 5.2.0

---

## 明确不做的内容

### 1. 新模型 / 模板
- ❌ 不新增训练模板
- ❌ 不新增评估模板
- ❌ 不新增 Mahjong 专用流水线

### 2. 主链重排
- ❌ 不重做 Evaluation → Artifact 流程
- ❌ 不重做 Promotion → Approval 流程
- ❌ 不重做 Seal 逻辑

### 3. Schema 重构
- ❌ 不新增表
- ❌ 不新增列
- ❌ 不修改现有表结构

### 4. UI 大翻新
- ❌ 不重写 Dashboard
- ❌ 不更换颜色体系
- ❌ 不重做 Layout

### 5. 新能力体系
- ❌ 不做权限系统
- ❌ 不做多租户
- ❌ 不做插件系统
- ❌ 不做云端部署
- ❌ 不做自动调度平台

### 6. 其他明确禁止
- ❌ 不改 Gate 判定语义
- ❌ 不破坏已有 API 格式
- ❌ 不删除现有文件
- ❌ 不做复杂聚合计算（只做查询关联）

---

## 本版允许的微量调整

仅限为 timeline / incident / correlation 目的：
- 新增查询 API（向后兼容）
- 新增关联查询（JOIN，不改源表）
- 前端增加 panel / drawer / 详情展示
- 新增 filter 参数（不影响默认行为）
