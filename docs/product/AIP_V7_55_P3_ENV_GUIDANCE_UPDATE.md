# AIP v7.55-P3 .env Guidance Update

**Date:** 2026-05-21
**Phase:** P3

---

## 1. Before

`.env.example` had:

- Header: `# ===== AGI Model Factory =====` (legacy name)
- Minimal safety note about `.gitignore`
- No version baseline marker
- No rule about secret rotation being owner action only

---

## 2. After

`.env.example` now has:

- Header: `# ===== OpenAIP (AIP) v7.55 =====` (current product name + version)
- Expanded safety rules:
  - `.env.local` is PRIVATE — never commit, share, or paste
  - Placeholder values will cause 401/403 errors
  - Real credentials must be generated by the OWNER, not automation
  - Secret rotation is a human-owner action, never automated
  - Reminder to run `pnpm run secret:scan`

---

## 3. Protected Files

`.env.local` was **not** read, modified, or printed. Its contents remain private.

---

## 4. Verdict

`.env.example` hardened with clearer security rules, current product name,
and version baseline. No secrets exposed or touched.
