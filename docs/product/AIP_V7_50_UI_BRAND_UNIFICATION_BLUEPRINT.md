# AIP v7.50 — D1 UI Brand Unification Blueprint

**Date:** 2026-05-20
**Phase:** D1
**Baseline HEAD:** `6eafdc5`
**Predecessor:** v7.50-D0 UI / Brand Product Polish Sweep (42 findings)

---

## 1. Objective

Define a unified brand strategy for the Web UI and plan fixes for the top 42 findings identified during the D0 sweep. This is a **documentation/planning phase only** — no source code is modified.

## 2. Brand Strategy

### 2.1 Target Brand Identity

| Element | Value | Rationale |
|---------|-------|-----------|
| **Product name** | AegisFlow Intelligence Platform (AIP) | Already established; consistent in HTML title, Dashboard, API info |
| **Short name** | AIP | Already used in sidebar footer (`AIP v{version}`), version labels, page subtitles |
| **CLI name** | `aip` (OpenAIP CLI) | CLI binary named `aip`; the naming `OpenAIP` does **not** exist anywhere in the codebase — no action needed |
| **Edition** | Community Edition | Correct as-is |
| **Logo** | "AG" topbar logo | Already in Layout.tsx:188 — stands for AegisFlow |

### 2.2 Stale Branding: Migration Plan

| Current Text | Occurrences | Recommended Replacement | Priority |
|--------------|-------------|------------------------|----------|
| `AGI Model Factory` in package.json name | 1 | `aegisflow` or `aip` | LOW (npm internal) |
| `AGI Model Factory` in plugin-runtime/plugin-sdk package.json descriptions | 2 | `AIP Plugin Runtime` / `AIP Plugin SDK` | LOW |
| `AGI Model Factory v6.x` comment headers | ~20 | `AIP Plugin Runtime v1.x` (or remove) | LOW |
| `AGI Factory Team` in plugin manifests | ~8 | `AegisFlow Team` | LOW |
| `*Generated by AGI Factory*` in output footers | ~4 | `*Generated by AegisFlow Intelligence Platform*` | LOW |
| `=== AGI Model Factory Smoke ===` in script headers | ~3 | `=== AIP Smoke ===` | LOW |
| `agi_factory_*` localStorage keys | ~19 | Keep as-is (migration would break existing user preferences); document as legacy keys | LOW |

### 2.3 "AegisFlow" Brand: Keep As-Is

All 14 BRAND occurrences of `AegisFlow` are correct and consistent:
- HTML title: `天枢智治平台 (AegisFlow Intelligence Platform)`
- Dashboard subtitle: `AegisFlow Intelligence Platform (AIP Community Edition) Overview`
- API info endpoint
- Generated output footers

No changes needed.

### 2.4 "OpenAIP" — Not Found

The naming pattern `OpenAIP` does not exist in the codebase. The CLI is named `aip`, and the external tool integration is named `OpenAxiom`. No action required.

### 2.5 "AGI_FACTORY" Environment Variable

The coexistence pattern `process.env.AGI_FACTORY_ROOT || process.env.AIP_REPO_ROOT` is already in place. The old env var is preserved for backward compatibility. Recommended: add a deprecation warning comment in source, but keep the fallback.

## 3. Version / Track / Release Gate Consistency Plan

### 3.1 Current State (from D0)

| Source | Value | Problem |
|--------|-------|---------|
| `appVersion.ts` | `APP_VERSION = 'v7.46.0'`, `BUILD_DATE = '2026.05.16'` | Used in Layout sidebar footer — correct for this codebase |
| `product-metadata-registry.ts` | `AIP_PRODUCT_VERSION = 'v7.25.2'`, `AIP_BUILD_DATE = '2026.05.19'` | **DISCREPANCY**: different version from appVersion.ts |
| `appMeta.ts:releaseUrl` | Points to `v7.3.0` release | **STALE**: 43 versions behind |
| UI sidebar footer | `AIP v7.46.0` / `Build 2026.05.16` | Correct per appVersion.ts |

### 3.2 Fix Plan

| # | Fix | Owner | Effort |
|---|-----|-------|--------|
| 1 | Synchronize `product-metadata-registry.ts`: update `AIP_PRODUCT_VERSION` to match `appVersion.ts` (v7.46.0 → will be v7.50.x after this release) | Plumber | Small |
| 2 | Synchronize `AIP_BUILD_DATE` in product-metadata-registry.ts to match BUILD_DATE | Plumber | Small |
| 3 | Update `releaseUrl` in `appMeta.ts` to point to the **latest actual release** (currently v7.3.0); add a comment noting it must be updated after each real release | Plumber | Small |
| 4 | Add a `AIP_TRACK` or `AIP_RELEASE_CHANNEL` constant (values: `stable`, `rc`, `dev`) and optionally display it in the sidebar footer or an "About" modal | Future release | Medium |
| 5 | Consider rendering `AIP_SEAL_STATUS` and `AIP_STAGE_C` status in a developer-visible location (e.g., Advanced Mode Readonly page) | Future release | Medium |

### 3.3 Sidebar Footer Display (Current)

```
AIP v7.46.0           ← APP_VERSION (correct)
Build 2026.05.16      ← BUILD_DATE (correct)
```

No changes needed to the footer itself. The version source (`appVersion.ts`) is the canonical one.

## 4. Dashboard Layout & Empty-State Copy Polish Plan

### 4.1 Layout Info Leak Fix

**Problem**: `Dashboard.tsx:664-665` and `FactoryStatus.tsx:571-572` render `<div>layoutMode: {layoutMode} · contentWidth: {Math.round(contentWidth)}px</div>` to all users.

**Fix**: Remove or guard behind a `process.env.NODE_ENV === 'development'` check.

| File | Lines | Action |
|------|-------|--------|
| `Dashboard.tsx` | 664-665, 671-672 | Guard or remove layoutMode/contentWidth debug output |
| `FactoryStatus.tsx` | 571-572, 583-584 | Guard or remove layoutMode/contentWidth debug output |
| `Dashboard.tsx` | 660-661 | Replace bare `Loading...` with skeleton/shimmer component |
| `Dashboard.css` | (various) | Remove dead CSS classes from old layout system |

### 4.2 Empty State Copy Standardization

**Problem**: 25+ empty state texts use inconsistent styles (emoji prefix, plain text, bilingual mix).

**Standard**: All empty states should follow one of two patterns:

| Pattern | Example | When |
|---------|---------|------|
| **Simple** | `暂无数据` / `No data` | Generic empty states |
| **Actionable** | `暂无知识条目。可点击"新增知识"创建第一条记录。` | Pages with a clear next-step CTA |

- Remove emoji prefixes from empty state text (emoji can stay in the `icon` prop of `EmptyState` component)
- Ensure all empty states have bilingual text (Chinese + English) where possible
- Standardize fallback in `EmptyState.tsx` component

## 5. LAN / Mobile / Sidebar Usability Fix Plan

### 5.1 Touch Support for Sidebar Resizer

**Problem**: Layout.tsx:387-395 — `.sidebar-resizer` only handles `MouseDown`/`MouseMove`/`MouseUp`. Touch devices cannot resize the sidebar.

**Fix**: Add `onTouchStart`/`onTouchMove`/`onTouchEnd` handlers parallel to the mouse handlers.

| File | Lines | Action |
|------|-------|--------|
| `Layout.tsx` | 387-395 | Add `onTouchStart` to `.sidebar-resizer` div |
| `Layout.tsx` | 147-163 | Add `touchmove`/`touchend` event listeners parallel to `mousemove`/`mouseup` |

### 5.2 Additional Mobile Polish

| Issue | Action | Effort |
|-------|--------|--------|
| `body.layout-resizing` global cursor override | Limit scope to sidebar only | Small |
| Content min-width: 320px | Reduce to `min-width: 280px` or remove | Small |
| No `overscroll-behavior` on sidebar | Add `overscroll-behavior: contain` to sidebar scroll container | Small |

## 6. Route-Level Feature Flag / Hidden Preview Safety Policy

### 6.1 Current Risk

- **40+ preview routes** are defined in `App.tsx` with **no** route-level feature flag or permission check
- **15+ Stage C routes** are live and functional despite `AIP_STAGE_C = 'disabled'` in metadata
- Any user who discovers the URL can access any preview route
- 5 readonly/debug pages are visible in the sidebar with confusing names (`-readonly` suffix exposed to users)

### 6.2 Route Safety Policy

| Principle | Rule |
|-----------|------|
| **All preview routes** MUST have a feature flag or gate check before rendering | Enforced by route wrapper or middleware |
| **Stage C routes** MUST return 404 or redirect when `AIP_STAGE_C === 'disabled'` | Enforced regardless of direct URL access |
| **Hidden direct routes** are acceptable IF they have a safety boundary | Must be documented in registry |
| **Sidebar-visible routes** must NOT contain `-readonly`, `-preview`, `-debug` in their display label | User-facing label should be clean |

### 6.3 Implementation Plan

| # | Action | Protection Level | Effort |
|---|--------|-----------------|--------|
| 1 | Create `<RouteGuard>` component that checks `FEATURE_FLAGS.STAGE_C_ENABLED` before rendering Stage C routes | Stage C routes | Medium |
| 2 | Wrap all preview routes with a `process.env.NODE_ENV === 'development'` guard or an explicit `AIP_ALLOW_PREVIEW_ROUTES` flag | Preview routes | Medium |
| 3 | Create a route metadata registry that maps each route to its exposure level and required gate | All routes | Large |
| 4 | Rename sidebar NavItem labels to remove `readonly`/`debug` suffixes (e.g., "Advanced Mode Preview" → "Advanced Mode"; "Mahjong Debug" → "Mahjong Lab") | Sidebar | Small |
| 5 | Document all hidden routes in `navigation-exposure-registry.ts` with `allowedNow: false` where applicable | Registry | Medium |

## 7. Cumulative Plan Summary

| Area | D0 Findings | D1 Actions Planned | Priority |
|------|-------------|-------------------|----------|
| Naming: stale AGI Model Factory | 49 occurrences | ~40 low-priority renames in package.json, comment headers, manifests | LOW |
| Naming: localStorage keys | 19 keys | Keep as legacy; document migration path | LOW |
| Version: stale releaseUrl | 1 HIGH | Update URL; add comment to refresh after release | **HIGH** |
| Version: discrepancy | 1 HIGH | Sync product-metadata-registry.ts with appVersion.ts | **HIGH** |
| Version: no Track/Gate badge | 1 MEDIUM | Add `AIP_TRACK` constant; optional UI display | MEDIUM |
| Dashboard: layout info leak | 2 MEDIUM | Guard or remove debug output | **HIGH** |
| Dashboard: loading skeleton | 1 LOW | Add skeleton/shimmer | LOW |
| Empty state consistency | 2 LOW | Standardize pattern; remove emoji prefixes | LOW |
| Mobile: sidebar resizer touch | 2 MEDIUM | Add touch event handlers | MEDIUM |
| Mobile: misc CSS fixes | 3 LOW | Reduce min-width, overscroll-behavior, scope cursor | LOW |
| Route safety: no feature flags | 3 MEDIUM | Create `<RouteGuard>`, wrap preview routes, rename sidebar labels | **HIGH** |
| Route safety: Stage C live routes | embedded in above | Block Stage C routes when `AIP_STAGE_C === 'disabled'` | **HIGH** |

## 8. Deliverables (this phase)

| Doc | Purpose |
|-----|---------|
| `AIP_V7_50_UI_BRAND_UNIFICATION_BLUEPRINT.md` | This document — overall strategy and plan |

## 9. Safety

- No source code modified in this phase
- No GitHub Release or tag created
- Stage C remains disabled
- Feature flags remain off
- No hidden previews exposed
- No DB writes or restores executed
