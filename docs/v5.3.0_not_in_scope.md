# v5.3.0 Not In Scope — 明确不纳入范围

**Date**: 2026-04-13  
**Version**: 5.3.0

---

## 明确不做的内容

### 1. 新模型 / 模板
- ❌ 不新增训练模板
- ❌ 不新增评估模板
- ❌ 不新增 Mahjong 专用流水线

### 2. 主链重排
- ❌ 不重做 Release → Seal 流程
- ❌ 不重做 Promotion → Approval 流程
- ❌ 不重做 Backup → Recovery 流程

### 3. Schema 重构
- ❌ 不新增表
- ❌ 不新增列（优先使用现有字段或 JSON 扩展）
- ❌ 不修改既有表结构

### 4. UI 大翻新
- ❌ 不重写 Dashboard
- ❌ 不更换颜色体系
- ❌ 不重做 Layout
- ❌ 不做 FactoryStatus 大翻修

### 5. 新能力体系
- ❌ 不做权限系统
- ❌ 不做多租户
- ❌ 不做插件系统
- ❌ 不做云端部署
- ❌ 不做自动调度平台
- ❌ 不做版本中心大重构

### 6. 其他明确禁止
- ❌ 不改 Gate 判定语义
- ❌ 不破坏既有 API 格式
- ❌ 不删除现有文件
- ❌ 不做复杂的版本中心重构

---

## 本版允许的微量调整

仅限为发布治理目的：
- 新增查询 API（向后兼容）
- 新增聚合查询（JOIN，不改源表）
- 前端增加 panel / drawer / 详情展示
- 新增 filter 参数（不影响默认行为）
- 使用现有 JSON 字段做扩展（不新增列）
